var persistence = exports;
persistence.engines = require('./engines');
persistence.resources = require('./resource');
persistence.resources = {};

persistence.setValidator = function (validator, options, callback) {
  if (typeof validator === 'jayschema')
    
}

persistence.setEngine = function(engine, options, callback) {
  if (typeof engine == 'string')
    
  else (use )
};

persistence.define = function (name, schema, callback) {
  if (typeof )
};